---
import { Image } from "astro:assets"

const team = [
  {
    photo: "https://placehold.co/400x400",
    rol: "Fundador y CEO",
    name: "Ricardo De La Torre",
    description:
      "Con más de 20 años de experiencia en buceo y formación en biología marina, Juan lidera nuestra empresa con pasión y experiencia.",
    linkedin: ""
  }
]
---

<section
  id="nuestro-equipo"
  class="bg-gradient-to-r from-blue-950 to-blue-800 py-16 text-white"
>
  <div class="container mx-auto px-5 xl:px-0">
    <div class="scroll-zone mx-auto mb-16 max-w-3xl text-center">
      <h2 class="scroll-float-in mb-6 text-3xl font-bold">
        Conoce a quienes hacen posible esta visión
      </h2>
      <p class="scroll-fade-in-up text-gray-300">
        Somos un equipo apasionado por el mar, con un firme compromiso con la
        preservación marina, la innovación y la educación desde el respeto y la
        empatía.
      </p>
    </div>

    <div class="scroll-zone grid gap-8 md:grid-cols-2 lg:grid-cols-4">
      {
        team.map(({ photo, rol, name, description, linkedin }) => (
          <div class="scroll-fade-in-up rounded-xl bg-white/10 p-6 text-center backdrop-blur-sm transition-colors hover:bg-white/20">
            <div class="relative mx-auto mb-6 h-40 w-40 overflow-hidden rounded-full border-4 border-white/30">
              <Image
                src={photo}
                alt={`${name} - Miembro del equipo`}
                width={160}
                height={160}
                class="h-full w-full object-cover"
              />
            </div>
            <h3 class="text-ibwt-secondary mb-1 text-xl font-bold">{name}</h3>
            <p class="mb-4 text-blue-50">{rol}</p>
            <p class="mb-4 text-sm text-gray-300">{description}</p>
            <div class="flex justify-center space-x-3">
              <a
                href={linkedin}
                target="_blank"
                rel="noopener noreferrer"
                class="text-white/70 hover:text-white"
              >
                {/* TODO: Linkedin  */}
                {/* <Icon
                  name="social-media/linkedin"
                  size={30}
                /> */}
              </a>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>
