<section class="py-16">
  <div class="container mx-auto px-5 xl:px-0">
    <div class="scroll-zone mx-auto mb-16 max-w-3xl text-center">
      <h2 class="text-ibwt-primary scroll-float-in mb-6 text-3xl font-bold">
        Sumérgete en nuestros recuerdos
      </h2>
      <p class="scroll-fade-in-up text-gray-700">
        Explora fragmentos visuales de nuestro recorrido. Cada imagen refleja
        momentos únicos que celebran la conexión entre el ser humano y el
        océano.
      </p>
    </div>

    <div class="grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">
      {
        Array.from({ length: 8 }, (_, index) => (
          <div class="scroll-zone image-zoom-out relative h-64 overflow-hidden rounded-lg">
            <img
              src="https://placehold.co/600x800"
              alt={`Galería ${index + 1}`}
              class="image-hover h-full w-full object-cover"
            />
          </div>
        ))
      }
    </div>
  </div>
</section>
