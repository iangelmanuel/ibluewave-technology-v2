---
import { Icon } from "astro-icon/components"

import { socialMediaIcon } from "@constant/social-media"
---

<div class="mt-8">
  <h4 class="mb-4 font-medium">Síguenos en nuestras redes sociales</h4>

  <div class="flex space-x-4">
    {
      socialMediaIcon.map(({ href, name }) => (
        <a
          href={href}
          target="_blank"
          rel="noopener noreferrer"
          class="text-ibwt-primary flex h-10 w-10 items-center justify-center rounded-full bg-blue-100 transition-colors hover:bg-blue-200"
        >
          <Icon
            name={name}
            size="20"
          />
        </a>
      ))
    }
  </div>
</div>
